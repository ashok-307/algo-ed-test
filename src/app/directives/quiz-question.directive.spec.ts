import { QuizQuestionDirective } from './quiz-question.directive';

describe('QuizQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new QuizQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
