import { QuizBlockDirective } from './quiz-block.directive';

describe('QuizBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new QuizBlockDirective();
    expect(directive).toBeTruthy();
  });
});
